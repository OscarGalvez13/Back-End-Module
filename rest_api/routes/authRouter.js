const express = require("express");
const router = express.Router();

const User = require("/"7module.exports = router;);

router.post("/auth", async (req, res) => {
  res.json({
    error: null,
    data: "Aqui va la data",
  });
});

module.exports = router;
